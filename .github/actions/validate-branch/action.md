## Conventions

❌ **Invalid branch name used:** `{{BRANCH_NAME}}`

Allowed conventions:

- `main`
- `staging`
- `rollback/<lowercase-name> (letters, numbers, dashes)`
- `topic/<lowercase-name> (letters, numbers, dashes)`
- `debug/<lowercase-name> (letters, numbers, dashes)`
- `tests/<lowercase-name> (letters, numbers, dashes)`